// SSE/HTTP transport proxy — placeholder for future implementation
// The stdio proxy covers the most common MCP transport (Claude Desktop, Cursor, Claude Code).
// SSE proxy would be needed for HTTP-based MCP servers.

/// SSE proxy is not yet implemented.
/// Use stdio proxy for most MCP server configurations.
pub fn sse_proxy_placeholder() {
    eprintln!("SSE proxy is not yet implemented. Use stdio transport.");
}
